import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.rockpaperscissors',
  appName: 'Rock Paper Scissors',
  webDir: 'dist'
};

export default config;
